spring:
  application:
    name: ucll-backend
  profiles:
    active: "${SPRING_PROFILES_ACTIVE:dev}"

server:
  port: ${PORT:3000}

cors:
  allowed-origins:
    - ${CORS_ALLOWED_ORIGINS}

application:
  security:
    jwt:
      secret-key: ${JWT_SECRET_KEY}